# Subfinder Results

This folder contains subdomain enumeration results from the Subfinder tool.

## File Format
- Default: Plain text files with one subdomain per line
- JSON: Structured data with additional metadata (when using -oJ flag)

## Example Output Structure
```
subdomain1.example.com
subdomain2.example.com
api.example.com
admin.example.com
```